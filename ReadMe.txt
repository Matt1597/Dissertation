First run generateData.py that will generate the linkage tables and related circles sizes. You can change the similarity matrix in generateData.py.

Then run Agglomerative.py this will create the agglomerative layout.

Finally Open each of the folders (force, pack, agglomerative) and open the html file in firefox.
